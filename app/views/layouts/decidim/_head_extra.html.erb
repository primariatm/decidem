<style type="text/css">

  @import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,100;0,400;0,500;0,600;0,700;1,400;1,600&display=swap');
  
  /* Variables */
  :root {
    /* Main blue */
    --primary: #003995;
    --primary-lightest: #0665FB;
    --primary-light: #004AC0;
    --primary-dark: #002E77;
    --primary-darkest: #002258;


    /* Secondary blue */
    --secondary: #1A009C;
    --secondary-lightest: #3108FC;
    --secondary-light: #2100C6;
    --secondary-dark: #15007D;
    --secondary-darkest: #0F005C;

    /* Other */
    --dark-pmt: #111111;
    --grey-dark: #4B4B4B;
    --grey-medium: #a1a1a2;
    --grey-light: #c7c8ca;

    /* Tertiary color -- yellow */
    --tertiary: #E1B500;
    --tertiary-lightest: #FFCD00;
    --tertiary-light: #FFCD00;
    --tertiary-dark: #B49100;
    --tertiary-darkest: #846A00;

    /* Complementary -- orange */
    --complementary: #E18F00;
    --complementary-lightest: #FFA200;
    --complementary-light: #FFA200;
    --complementary-dark: #B47300;
    --complementary-darkest: #845400;
  }

  /* General */
  * { 
    font-family: 'IBM Plex Sans', sans-serif !important;
  }

  a:not(.button), .card__link {
    color: var(--primary) !important;
  }

  * {
    transition: 0.25s linear;
  }

  /* Navigation */
  .title-bar, div.navbar {
    background-color: var(--primary) !important;
  }

  .title-bar a, .navbar a {
    color: var(--grey-light) !important;
  }
  .main-nav__link a:hover {
    background: var(--primary-lightest) !important;
    color: #FFF !important;
  }
  .main-nav__link--active {
    background: var(--primary-light) !important;
  }
  .main-nav__link--active a { font-weight: 600 !important; }

  .title-bar a:hover .navbar a:hover {
    color: #FFFFFF !important;
  }

  .hero::after { background: transparent !important; }

  .hero__container .text-highlight.hero-heading {
    text-align: right !important;
    color: var(--primary) !important;
    text-shadow: none !important;
    font-size: 4rem !important;
  }

  .hero__container div.row div.columns.small-centered.large-10 {
    width: 70%;
  }

  .hero__container .text-highlight.hero-heading::after {
    content: '';
    display: block;
    height: 4rem;
    width: 60%;
  }

  .hero__container .row:nth-child(2) > div.columns.small-centered,
  .hero__container .row:first-child > div.columns.small-centered {
    float: right !important;
  }

  #content section.hero.home-section {
    background-size: 70%;
    background-repeat: no-repeat;
    background-position: 35%;
    background-color: transparent !important;
  }

  .header .navbar nav.main-nav li a.main-nav__link:hover {
    color: #FFF;
  }
  .topbar__search .input-group {
    border: 1px solid var(--grey-light);
    border-radius: 3px;
  }
  .topbar__search .input-group #term,
  .topbar__search .input-group #submit {
    background: transparent !important;
    color: var(--grey-light) !important;
  }
  .topbar__search .input-group #term:focus {
    color: #FFF !important;
  }
  .topbar__search .input-group #term:focus::placeholder {
    color: rgba(255, 255, 255, 0.5) !important;
  }
  .heading3 { font-weight: 700 !important; }

  /* Footer */
  .main-footer, .mini-footer { 
    background-color: var(--dark-pmt) !important;
    color: var(--grey-light) !important;
  }

  .main-footer a, .mini-footer a {
    color: var(--grey-light) !important;
  }

  .main-footer a:hover, .mini-footer a:hover {
    color: #FFFFFF !important;
  }
  
  section.extended .home-section h2 {
    font-weight: 800 !important;
  }

  .language-choose .menu.is-dropdown-submenu > li a {
    color: var(--primary) !important;
  }

  /* Extra */

  .card.card--mini {
    transition: 0.25s ease-in-out;
  }

  .card.card--mini:hover {
    transform: translate3D(0, -5px, 0);
  }

</style>